pipeline {
    agent any
    
    stages {
        stage("SCM checkout") {
            steps {
                git "https://https://github.com/moolanar/mar-repo"
            }
        }
        
        stage("Execute Ansible") {
            steps {
                ansiblePlaybook inventory: 'inv.ynl', playbook: 'pngc-playbk.yml', vaultCredentialsId: 'ec2-user', vaultTmpPath: ''         
               
        }    
    }
}
